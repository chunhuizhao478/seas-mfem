# Round-8 fix report — 2026-04-24

All four Round-8 findings addressed. The driver is now ready for
Frontera submission in the order documented below.

## Work list (resolved)

| ID | Sev | Resolution | Files |
|----|-----|------------|-------|
| R8-001 | MOD | Replaced hardcoded `Actual*Tag()` functions with `enum class DispatchedSolver { Brent, NewtonRaphsonStable, NewtonRaphsonLegacy, Hybrid }`, `DispatchedIterator`, `DispatchedLaw`. Tag values now derived via `TagOf(enum)` / `BannerOf(enum)` switches with no default case — `-Wswitch` warns on missing cases. `GetDispatched*` functions take the CLI value as input; today they unconditionally return the option-(b) enum, but when the iterator is wired they become CLI-dependent with no need to edit `TagOf` / `BannerOf`. | `drivers/tpv104_driver.cpp` |
| R8-002 | LOW | `(void)MapSolver(friction_solver)` replaced with `const FrictionSolver::Method method = MapSolver(friction_solver); (void)method;` — future wiring needs a two-step edit (remove `(void)method;` AND pass `method` to the iterator), but the linkage point now has a named local that grep/IDE can find. | `drivers/tpv104_driver.cpp` |
| R8-003 | LOW | `TPV104SurfaceStationWriter::Close()` added symmetric to `TPV104StationWriter::Close`. Driver now calls `surface_writer.Close()` on BOTH exit paths (init-only and end-of-sim) BEFORE `MPI_Finalize`. | `dynamic/tpv104_setup.hpp`, `drivers/tpv104_driver.cpp` |
| R8-004 | LOW | `--verify-dispatch` `[dispatch]` emission moved outside the `rank==0` banner guard. Each line is now prefixed with `rank=<N>` so interleaved multi-rank output on Frontera is parseable. Smoke test + banner-sanity sbatch grep targets updated to match the `rank=0 …` format. | `drivers/tpv104_driver.cpp`, `tests/unit/test_tpv104_smoke.cpp`, `jobs/tpv104/tpv104_build_banner_sanity.sbatch` |

## Banner — before / after R8

```
--- R7 (already honest, R8 refactor-only) ---
Time integrator: ADER-O2 (one-shot via wave.AdvanceADER)
Fault iterator: one-shot (sub-step iterator NOT wired; --fault-iterator flag IGNORED — R7-001)
Friction solver: Brent (hard-coded via EvaluateADERTotal; --friction-solver flag IGNORED)
Friction law: slip-SRW (ψ-space, macro-step analytic UpdateStateAnalyticSlipLawSRW)
Nucleation: enabled (TPV104, macro-step incremental telescoping)
CLI parsed (banner-only, not dispatched): friction_solver=newton-stable, fault_iterator=substep, fric_law=slip-srw
```

```
--- R8 [dispatch] lines (per-rank format) ---
[dispatch] rank=0 friction_solver_actual=brent
[dispatch] rank=0 fault_iterator_actual=oneshot
[dispatch] rank=0 friction_law_actual=slip-srw
```

## Test results post-R8

```
seas_test_tpv104_smoke               : 45/45  PASS
seas_test_tpv104_friction_solver_newton : 25/25 PASS
seas_test_tpv104_mesh                : 13/13  PASS
seas_test_tpv104_normal_sign         : 17/17  PASS
seas_test_tpv104_nucleation          : 21/21  PASS
seas_test_tpv104_params              : 36/36  PASS
seas_test_tpv104_probe_format        : 13/13  PASS
seas_test_tpv104_setup               : 38/38  PASS
seas_test_tpv104_substep_iterator    : 40/40  PASS
seas_test_friction_solver_stable     : 16/16  PASS
-----------------------------------------
Total TPV104-adjacent                : 264/264 PASS
```

Regression spot-checks:
```
seas_test_ader_tpv102_smoke          : 4/4   PASS
seas_test_R001_driver_defaults_to_ader : 3/3 PASS
seas_test_R008_fault_branch_preserved  : 4/4 PASS
seas_test_bp5_parallel_smoke         : 17/17 PASS
seas_test_ader_linear_wave_equivalence : 4/4 PASS
```

Pre-existing BP5 failure unrelated to R7/R8 (tracked separately):
```
seas_test_bp5_integration            : ValidateFacetBCTables attr-3 assertion
```

## Local P2_D re-verification

```
./seas_tpv104_driver --mesh tpv102/mesh/tpv102_1000m.msh --tfinal 0.0 --output-dir /tmp/tpv104_init_r8
P2_D: 9/9 fault stations PASS, max err=2.630e-09   (threshold 1e-8)
```

## Round-8 checklist closure

- [x] **R8-001**: enum + switch replace hardcoded constants; `-Wswitch` catches missing cases when CLI becomes live.
- [x] **R8-002**: `method` preserved as named local for future iterator wiring.
- [x] **R8-003**: `TPV104SurfaceStationWriter::Close()` added; driver calls it before `MPI_Finalize` on every exit path.
- [x] **R8-004**: per-rank `[dispatch]` emission; all consumers (smoke test, banner sbatch) updated.

## Ready for re-review: YES
